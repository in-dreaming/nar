# NAR 任务索引

实现 agent 先读 `setup.md`，再只执行脚本分配的单个编号任务。任务必须按编号顺序提交；提交主题固定为 `<task-id> done`。

任务 00-06 已完成。spindle 已完成开发，任务 07 将现有 NAR 基线破坏性迁移到 spindle Runtime/Executor/Clock/Task/Resource Graph；任务 08-12 建立在新基线上。spindle ECS 与 Durable Workflow 不属于 NAR 首版依赖。
